{
  "steps": [
    {
      "description": "Pulling latest changes",
      "type": "shell",
      "command": "cd {{$home}} && git pull --rebase 2>&1 || echo 'Not a git repository'"
    },
    {
      "description": "Updating dependencies",
      "type": "shell",
      "command": "cd {{$home}} && source .venv/bin/activate && pip install --upgrade opencv-python numpy pillow flask flask-cors 2>&1"
    },
    {
      "description": "Update completed",
      "type": "shell",
      "command": "echo '✅ Update complete!'"
    }
  ]
}
