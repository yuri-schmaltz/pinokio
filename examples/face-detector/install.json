{
  "steps": [
    {
      "step": "1/6",
      "description": "Creating Python virtual environment",
      "type": "shell",
      "command": "cd {{$home}} && if [ ! -d '.venv' ]; then python3 -m venv .venv; fi"
    },
    {
      "step": "2/6",
      "description": "Activating virtual environment",
      "type": "shell",
      "command": "cd {{$home}} && source .venv/bin/activate"
    },
    {
      "step": "3/6",
      "description": "Installing base dependencies",
      "type": "shell",
      "command": "cd {{$home}} && source .venv/bin/activate && pip install --upgrade pip setuptools wheel 2>&1"
    },
    {
      "step": "4/6",
      "description": "Installing dependencies (OpenCV, Flask)",
      "type": "shell",
      "command": "cd {{$home}} && source .venv/bin/activate && pip install opencv-python numpy pillow flask flask-cors 2>&1"
    },
    {
      "step": "5/6",
      "description": "Downloading sample image",
      "type": "shell",
      "command": "mkdir -p {{$home}}/input && cd {{$home}}/input && if [ ! -f sample.jpg ]; then curl -s -o sample.jpg 'https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=400' || echo 'Could not download sample image'; fi"
    },
    {
      "step": "6/6",
      "description": "Installation completed",
      "type": "shell",
      "command": "echo '✅ Installation complete! You can now start the app.'"
    }
  ]
}
