{
  "steps": [
    {
      "description": "Checking NVIDIA GPU (CUDA)",
      "type": "shell",
      "command": "nvidia-smi 2>/dev/null && echo '✅ NVIDIA GPU found' || echo '⚠️ No NVIDIA GPU detected'"
    },
    {
      "description": "Checking AMD GPU (ROCm)",
      "type": "shell",
      "command": "rocm-smi 2>/dev/null && echo '✅ AMD GPU found' || echo '⚠️ No AMD GPU detected'"
    },
    {
      "description": "Checking OpenCV GPU support",
      "type": "shell",
      "command": "cd {{$home}} && source .venv/bin/activate && python3 -c \"import cv2; print(f'OpenCV GPU Support: {cv2.cuda.getCudaEnabledDeviceCount() if hasattr(cv2.cuda, 'getCudaEnabledDeviceCount') else 'N/A'}')\" 2>/dev/null || echo 'OpenCV GPU checking not available'"
    },
    {
      "description": "Checking OpenCV version",
      "type": "shell",
      "command": "cd {{$home}} && source .venv/bin/activate && python3 -c \"import cv2; print(f'OpenCV: {cv2.__version__}')\" 2>/dev/null || echo 'OpenCV not installed'"
    },
    {
      "description": "GPU Check completed",
      "type": "shell",
      "command": "echo '✅ GPU diagnostics finished. App uses CPU by default (GPU acceleration with CUDA optional).'"
    }
  ]
}
